{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Catégorie</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"class="btn btn-success text-white mdi mdi-lead-pencil"> Editer</a> <br><br> {{ include('categorie/_delete_form.html.twig') }}
    <br><a href="{{ path('categorie_index') }}"><h4 class="mdi mdi-arrow-left-box">Retour</h4></a>

    
{% endblock %}
